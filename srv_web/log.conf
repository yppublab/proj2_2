server {
    listen 80 default_server;
    server_name site.local;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }

    location = /log/ {
        return 302 /log/access.log;
    }

    location /log/ {
        alias /;
        autoindex on;
        autoindex_exact_size off;
        autoindex_localtime on;
        sendfile on;
        try_files $uri =404;
    }
}
